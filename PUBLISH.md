# Publish Configuration for vendure

<!-- Auto-generated by publish-manager skill on 2026-06-16. Edit if the workflow changes. -->

## Package Manager

- **Type**: `pub.dev`
- **Packages**: Single package — `vendure`
- **Public packages**: `vendure` (published to pub.dev)
- **Private packages**: None

## Scripts

- **Pre-publish**: Not configured — version and changelog updated manually
- **Publish**: `dart pub publish` — publishes to pub.dev
- **Post-publish**: Not configured — tag created manually after publish

## Workflow

1. Update `pubspec.yaml` version
2. Update `CHANGELOG.md` with new version entry
3. Commit and push to a release branch (e.g., `publish-<version>`)
4. `dart pub publish`
5. `git tag v<version>` and `git push origin v<version>`

## Notes

- The package version in `pubspec.yaml` is the source of truth.
- CHANGELOG is maintained manually per release.
- There are no pre-publish or post-publish automation scripts.
- Validate with `dart pub publish --dry-run` before actual publish.
